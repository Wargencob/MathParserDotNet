## 1.About library
- A simple library that computes an expression coming from a string
- The principle of operation: an expression string is input, decomposed into a set of tokens, converted into reverse Polish notation using the Shunting Yard algorithm, an abstract syntax tree is built and the expression is calculated.
## 2. Functionality
- Working with integer values and simple binary operators: +, -, *, /, ^
